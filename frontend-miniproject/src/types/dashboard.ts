export interface DashboardSummary {
    totalEvents: number;
    totalOrders: number;
    totalProfit: number;
    totalTickets: number;
  }
  
  export interface ChartData {
    name: string;
    total: number;
  }